def count_digits_letters(s):
    digits = sum(c.isdigit() for c in s)
    letters = sum(c.isalpha() for c in s)
    return letters, digits

# Sample Data
s = "Python 3.2"
letters, digits = count_digits_letters(s)
print(f"Letters: {letters}")  # 6
print(f"Digits: {digits}")    # 2
